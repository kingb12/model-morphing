
	i = 0
	for rxn in trans_model['reactions']:
		if rxn['reaction'] not in model_rxn_ids:
			print "error: you don't understand translate model"
		if len(rxn['features']) == 0:
			i += 1	
	j = 0
	for rxn in model['reactions']:
		if len(rxn['features']) == 0:
			j += 1
	print "Num Rxns in Trans Model: " + str(len(trans_model['reactions']))
	print "Num no-ftr rxns in trans: " + str(i) + " Num no-ftr rxns in model: " + str(j)
	print "Num recon rxns: " + str(len(rxn_labels['recon']))
	print "Num Model rxns: " + str(len(model['reactions']))
